using NSubstitute;
//using UnityEngine;
using NUnit.Framework;
using Shouldly;

namespace Tests.Mathf
{
    class MathfTests
    {
/*		[Test]
        public void TestSmoothDamp()
        {
            using (Substitute.ForStatic<Time>())
            {
                // arrange
                Time.deltaTime.Returns(100);

                // act
                var value = UnityEngine.Mathf.SmoothDamp(0, 1, 1);

                // assert
                value.ShouldBeInRange(0.9f, 1.1f);
            }
        }*/
    }
}
